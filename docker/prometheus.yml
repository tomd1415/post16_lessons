# Prometheus Configuration
# Scrapes metrics from the TLAC application

global:
  scrape_interval: 15s      # How often to scrape targets
  evaluation_interval: 15s  # How often to evaluate rules
  scrape_timeout: 10s       # Timeout for each scrape

  external_labels:
    monitor: 'tlac-monitor'
    environment: 'production'

# Alertmanager configuration (optional)
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets:
#             - 'alertmanager:9093'

# Load rules once and periodically evaluate them
# rule_files:
#   - "alerts/*.yml"

# Scrape configurations
scrape_configs:
  # Scrape Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Scrape TLAC application metrics
  - job_name: 'tlac-api'
    metrics_path: '/metrics'
    scrape_interval: 10s
    static_configs:
      - targets: ['api:8000']
        labels:
          service: 'tlac-api'
          instance: 'api-primary'

  # Scrape PostgreSQL exporter (if enabled)
  # - job_name: 'postgres'
  #   static_configs:
  #     - targets: ['postgres-exporter:9187']
  #       labels:
  #         service: 'postgres'

  # Scrape Caddy exporter (if enabled)
  # - job_name: 'caddy'
  #   static_configs:
  #     - targets: ['caddy:2019']
  #       labels:
  #         service: 'caddy'
